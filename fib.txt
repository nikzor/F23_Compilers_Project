routine fibonacci(n: integer): integer is
    if n <= 1 then
        return n;
    else
        return fibonacci(n - 1) + fibonacci(n - 2);
    end;
end;

var n: integer is 10;
var fib: integer;
fib := fibonacci(n);